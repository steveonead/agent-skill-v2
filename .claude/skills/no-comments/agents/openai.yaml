interface:
  display_name: "no-comments"
  short_description: "Audit comments without editing code"
  default_prompt: "Use $no-comments to audit comments in the current diff and return a strict read-only report."
policy:
  allow_implicit_invocation: true
