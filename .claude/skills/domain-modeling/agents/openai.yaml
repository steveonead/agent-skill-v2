interface:
  display_name: "domain-modeling"
  short_description: "Sharpen domain language and durable decisions"
  default_prompt: "Use $domain-modeling to clarify domain language and decide whether to record an architectural decision."
policy:
  allow_implicit_invocation: true
