interface:
  display_name: "ito-handoff"
  short_description: "Prepare focused handoffs for next sessions"
  default_prompt: "Use $ito-handoff to prepare the current work for the next session."

policy:
  allow_implicit_invocation: false
