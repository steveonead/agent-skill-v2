interface:
  display_name: "batch-grilling"
  short_description: "Stress-test thinking a round of questions at a time"
  default_prompt: "Use $batch-grilling to stress-test a plan with a full round of questions."

policy:
  allow_implicit_invocation: true
