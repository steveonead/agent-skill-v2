interface:
  display_name: "artifact-visualizer"
  short_description: "Render information as a single-file HTML visual reference"
  default_prompt: "Use $artifact-visualizer to render this information as a grounded single-file HTML visual reference."
policy:
  allow_implicit_invocation: true
