interface:
  display_name: "artifact-visualizer"
  short_description: "Create disposable interactive HTML visualizations"
  default_prompt: "Use $artifact-visualizer to turn this information into a disposable interactive HTML reference."
policy:
  allow_implicit_invocation: true
