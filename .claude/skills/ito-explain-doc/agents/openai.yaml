interface:
  display_name: "ito-explain-doc"
  short_description: "Turn a document into a visual explainer"
  default_prompt: "Use $ito-explain-doc to turn this document into a visual HTML explainer, using Traditional Chinese unless another language is requested."
policy:
  allow_implicit_invocation: false
