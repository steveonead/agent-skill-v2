interface:
  display_name: "implement-review"
  short_description: "Review a diff on standards and spec"
  default_prompt: "Use $implement-review to review the changes since main on standards and spec."
policy:
  allow_implicit_invocation: true
