interface:
  display_name: "visual-explainer"
  short_description: "Turn established context into a visual explanation"
  default_prompt: "Use $visual-explainer to turn the supplied context into a relationship-first HTML explainer."
policy:
  allow_implicit_invocation: true
