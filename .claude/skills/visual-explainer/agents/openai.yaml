interface:
  display_name: "visual-explainer"
  short_description: "Turn supplied context into a visual explanation"
  default_prompt: "Use $visual-explainer to turn the supplied context into an HTML explainer that is 90% picture."
policy:
  allow_implicit_invocation: true
