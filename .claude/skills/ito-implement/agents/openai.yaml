interface:
  display_name: "ito-implement"
  short_description: "Implement test-first, then review code and comments"
  default_prompt: "Use $ito-implement to build this ticket test-first, review it, and audit its comments."
policy:
  allow_implicit_invocation: false
