interface:
  display_name: "grilling"
  short_description: "Stress-test thinking one question at a time"
  default_prompt: "Use $grilling to stress-test a plan one question at a time."

policy:
  allow_implicit_invocation: true
